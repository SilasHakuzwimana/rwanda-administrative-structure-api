services:
  - type: web
    name: RWANDA_ADMINISTRATIVE_STRUCTURE_APIs
    runtime: python
    buildCommand: pip install --upgrade pip && pip install -r requirements-prod.txt
    startCommand: gunicorn your_project.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
